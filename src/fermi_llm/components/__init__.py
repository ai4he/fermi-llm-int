"""First-party components.

Each subpackage holds one kind of module. They are ordinary plugins: nothing
in core references them by name, and a deployment can disable any of them in
``configs/plugins.toml``.
"""
